# Обязательные (замени значениями)
GHCR_OWNER=your-gh-username-or-org
API_HOST=api.example.com

# CORS: список через запятую (фронт, админка и пр.)
CORS_ALLOWED_ORIGINS=https://example.com,https://admin.example.com

# Postgres (если используешь встроенный сервис db)
POSTGRES_USER=memeup
POSTGRES_PASSWORD=change_me_strong
POSTGRES_DB=memeup

# JWT
JWT__Issuer=memeup
JWT__Audience=memeup_frontend
JWT__Key=PLEASE_GENERATE_A_LONG_RANDOM_48+_CHARS
JWT__LifetimeMinutes=60

# Admin seed
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!

# Если используешь внешний Postgres — удали секцию db в compose и задай ConnectionStrings напрямую
